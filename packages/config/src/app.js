/** Canonical app slug for URLs and telemetry (not localized). */
export const APP_SLUG = 'procraft';

/** Default landing domain (production); overrides via env where needed). */
export const DEFAULT_PUBLIC_DOMAIN = 'procraft.uz';
