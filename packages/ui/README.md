# `@procraft/ui`

Headless-but-styled building blocks referencing design tokens (`@procraft/config`).

## Rules

No data fetching (`@tanstack/react-query`), no `@procraft/services` / `@procraft/api`, no axios.
