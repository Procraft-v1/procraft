# `@procraft/store`

Redux Toolkit for **pure UI/client state**.

## Allowed

Sidebar visibility, density, theme toggle, transient auth UI flows (busy flags).

## Forbidden

Server cache, JWTs / refresh payloads, persisted credentials, hydrated profile entities as source of truth (use TanStack Query).
