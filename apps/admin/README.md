# @procraft/admin — admin.procraft.uz

Next.js (App Router) admin stats panel. Single client screen, cookie session
via `/admin/login`, `noindex`.

`pnpm dev` → http://localhost:5176 · prod port 3003 (Docker).

Rollback target: [apps/admin-legacy](../admin-legacy/).
